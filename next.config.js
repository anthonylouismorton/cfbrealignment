/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['upload.wikimedia.org', 'collegepolltracker.com','1000logos.net', 'www.pulf.org', 'i.pinimg.com'], // Add the relevant domains here
  },
};

module.exports = nextConfig;
